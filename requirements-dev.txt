-r requirements.txt
pytest>=9.1.1
# Powers the browser-based smoke checks in tests/test_console_smoke.py and
# tests/test_accessibility_smoke.py. Installing this package is not enough on
# its own — a browser binary is also required (`playwright install chromium`);
# both tests skip themselves when it hasn't been run, so this alone does not
# change CI's `pytest` outcome.
playwright
